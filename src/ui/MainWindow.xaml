<Window x:Class="Steganography.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Steganography"
        mc:Ignorable="d"
        Title="Jon's Steganography Project" Height="900" Width="1600">
    <Grid>
        
        <Grid.ColumnDefinitions>
            <!-- Define the columns in a ratio of 2:1 -->
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <!-- stegImage will be loaded dynamically -->
        <Image Name="stegImage" Height="400" Width="400" Grid.Column="0" Grid.Row="0"/>
        <Button Grid.Column="0" Grid.Row="1" Click="SelectPicture" Height="Auto" Width="Auto" HorizontalAlignment="Center" VerticalAlignment="Center">Test Button</Button>

    </Grid>
</Window>
